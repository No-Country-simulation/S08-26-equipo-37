generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// Business models are intentionally deferred until the real dataset and domain decisions are known.
